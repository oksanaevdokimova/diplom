from typing import Any  # аннотация типа для значений в словаре настроек

CONNECTION_DEFAULTS: dict[str, Any] = {  # запасной профиль подключения, если local.json недоступен или битый
    "transport": "tcp",  # тип канала по умолчанию: сеть TCP
    "host": "127.0.0.1",  # адрес для TCP (loopback — локальная машина)
    "port": 9000,  # номер TCP-порта по умолчанию
    "device": "COM1",  # имя COM-порта по умолчанию (Windows; на Mac заменят в local.json)
    "baudrate": 115200,  # скорость последовательного порта, бод
    "read_timeout": 1.0,  # таймаут чтения, секунды (для будущей реализации транспорта)
    "write_timeout": 1.0,  # таймаут записи, секунды
}
